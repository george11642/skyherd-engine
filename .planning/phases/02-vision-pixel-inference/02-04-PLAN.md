---
phase: 02-vision-pixel-inference
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - src/skyherd/vision/heads/pinkeye.py
  - src/skyherd/vision/preprocess.py
  - src/skyherd/vision/detector.py
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04]
tags: [vision, pixel-inference, pinkeye, mobilenet]
must_haves:
  truths:
    - "`Pinkeye.classify(cow, frame_meta)` performs real pixel-level inference when `frame_meta['raw_path']` is present (loads PNG, crops eye region, preprocesses, runs MobileNetV3-Small forward, maps argmax to Severity)"
    - "When `frame_meta['raw_path']` is missing or unreadable, Pinkeye falls back to the original rule-based severity mapping (preserves existing test behavior + Head ABC 'tolerate missing keys' contract)"
    - "The pixel head produces `DetectionResult.bbox = (x0, y0, x1, y1)` in raw 640x480 pixel coords whenever it returns a non-None detection via the pixel path"
    - "The reasoning string always contains `pinkeye.md` (preserves `test_reasoning_contains_skill_reference`) and always contains the cow's tag (preserves `test_reasoning_contains_cow_tag`)"
    - "Model is loaded exactly once per process via `functools.lru_cache(maxsize=1)` — no per-call load cost"
    - "Inference is deterministic: two back-to-back `classify()` calls on the same cow + same frame_meta return `DetectionResult` instances that compare equal via `.model_dump()` (excluding `timestamp`)"
    - "Other 6 disease heads (bcs, brd, foot_rot, heat_stress, lsd, screwworm) are UNCHANGED and continue to produce their existing detections"
  artifacts:
    - path: "src/skyherd/vision/preprocess.py"
      provides: "Shared PNG -> tensor helpers (load_frame_as_array, crop_region, array_to_tensor)"
      min_lines: 30
    - path: "src/skyherd/vision/detector.py"
      provides: "Geometric cow-bbox + eye-crop-bbox from world coords (cow_bbox_in_frame, eye_crop_bbox)"
      min_lines: 40
    - path: "src/skyherd/vision/heads/pinkeye.py"
      provides: "Pixel inference Pinkeye head — Head ABC subclass, pixel classify with rule fallback"
      contains: "mobilenet_v3_small"
      min_lines: 120
  key_links:
    - from: "src/skyherd/vision/heads/pinkeye.py"
      to: "src/skyherd/vision/_models/pinkeye_mbv3s.pth"
      via: "importlib.resources + torch.load(weights_only=True)"
      pattern: "pinkeye_mbv3s.pth"
    - from: "src/skyherd/vision/heads/pinkeye.py"
      to: "src/skyherd/vision/pipeline.py (frame_meta['raw_path'])"
      via: "dict lookup"
      pattern: "raw_path"
    - from: "src/skyherd/vision/heads/pinkeye.py"
      to: "src/skyherd/vision/detector.py"
      via: "cow_bbox_in_frame, eye_crop_bbox"
      pattern: "cow_bbox_in_frame|eye_crop_bbox"
    - from: "src/skyherd/vision/heads/pinkeye.py"
      to: "src/skyherd/vision/preprocess.py"
      via: "load_frame_as_array, crop_region, array_to_tensor"
      pattern: "load_frame_as_array|array_to_tensor"
---

<objective>
Rewrite `src/skyherd/vision/heads/pinkeye.py` to perform real pixel-level inference on rendered PNG frames using the MobileNetV3-Small weights shipped by Plan 03. Add two small helper modules (`preprocess.py`, `detector.py`) that encapsulate PNG->tensor preprocessing and world-coord-to-pixel-bbox reverse projection.

Purpose: This is the core narrative artifact of Phase 2 — the "Keep Thinking $5k" pattern match. Judges reviewing `src/skyherd/vision/heads/pinkeye.py` must see real pixel CNN inference (PIL load + torchvision forward + softmax) against the deterministic rendered frame, not a threshold classifier on `Cow.ocular_discharge`. Preserving the rule fallback (when `raw_path` absent) keeps ALL existing pinkeye unit tests passing through Wave 3's test updates (Plan 05), while enabling pixel inference in real pipeline runs.

Output: Rewritten `pinkeye.py` with pixel path + rule fallback; new `preprocess.py` + `detector.py` utility modules; model load is lazily cached and deterministic; bbox field populated on pixel-path detections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-vision-pixel-inference/02-RESEARCH.md
@.planning/phases/02-vision-pixel-inference/02-PATTERNS.md
@.planning/phases/02-vision-pixel-inference/02-01-SUMMARY.md
@.planning/phases/02-vision-pixel-inference/02-03-SUMMARY.md
@src/skyherd/vision/heads/base.py
@src/skyherd/vision/heads/pinkeye.py
@src/skyherd/vision/result.py
@src/skyherd/vision/renderer.py
@src/skyherd/vision/pipeline.py
@src/skyherd/world/cattle.py
@src/skyherd/world/terrain.py
@src/skyherd/edge/detector.py
@src/skyherd/vision/_models/WEIGHTS.md
@skills/cattle-behavior/disease/pinkeye.md

<interfaces>
Head ABC contract (must preserve):

```
class Head(ABC):
    @property @abstractmethod
    def name(self) -> str: ...
    def should_evaluate(self, cow: Cow, frame_meta: dict[str, Any]) -> bool: ...
    @abstractmethod
    def classify(self, cow: Cow, frame_meta: dict[str, Any]) -> DetectionResult | None: ...
```

frame_meta contract (from Plan 01):

```
frame_meta = {
    "trough_id": str,
    "temp_f": float,
    "raw_path": Path | None,  # Optional. Pixel head uses it; rule heads ignore.
    # ... any frame_meta_override additions
}
```

Existing rule head severity tiers (preserved for rule-fallback + as pixel-head class labels):
- discharge <= 0.4 and no flag -> None (no detection)
- discharge in (0.4, 0.6) -> "watch"
- discharge in [0.6, 0.8) -> "log"
- discharge in [0.8, 1.0] -> "escalate"
- flag "pinkeye" present and discharge <= 0.4 -> "log" (flag override)

Severity literal from `src/skyherd/vision/result.py`:
```
Severity = Literal["watch", "log", "escalate", "vet_now"]
```

Model architecture (matches state_dict shipped by Plan 03):
```
from torchvision.models import mobilenet_v3_small, MobileNet_V3_Small_Weights
model = mobilenet_v3_small(weights=MobileNet_V3_Small_Weights.IMAGENET1K_V1)
model.classifier[3] = nn.Linear(model.classifier[3].in_features, 4)
```

Class -> Severity mapping (indices match Plan 03 label_for_cow):
- 0 -> None (healthy, no detection)
- 1 -> "watch"
- 2 -> "log"
- 3 -> "escalate"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add src/skyherd/vision/preprocess.py + src/skyherd/vision/detector.py utility modules</name>
  <files>src/skyherd/vision/preprocess.py, src/skyherd/vision/detector.py</files>
  <behavior>
    - `preprocess.py` exports: `load_frame_as_array(path: Path) -> np.ndarray`, `crop_region(arr: np.ndarray, bbox: tuple[int,int,int,int]) -> np.ndarray`, `array_to_tensor(crop: np.ndarray) -> torch.Tensor`, plus a module-level `_PREPROCESS` transforms.Compose that normalizes to ImageNet stats.
    - `array_to_tensor(empty_or_tiny_crop)` handles degenerate crops (shape < 10x10) by resizing up; must never crash on small crops.
    - `detector.py` exports: `cow_bbox_in_frame(cow: Cow, bounds_m: tuple[float,float]) -> tuple[int,int,int,int]`, `eye_crop_bbox(cow_bbox, cow) -> tuple[int,int,int,int]`. Coordinates in raw pixel space (640x480). All formulas match `renderer._world_to_frame` + `_draw_cow_blob` byte-for-byte (so the classifier sees the exact eye region the renderer drew).
    - Both modules pass pyright + ruff without suppressions.
  </behavior>
  <action>
    Create `src/skyherd/vision/preprocess.py`:

    Imports (exact, at top):
    - `from __future__ import annotations`
    - `from pathlib import Path`
    - `import numpy as np`
    - `import torch`
    - `from PIL import Image`
    - `from torchvision import transforms`

    Module body:
    ```
    _PREPROCESS = transforms.Compose([
        transforms.Resize((224, 224)),
        transforms.ToTensor(),
        transforms.Normalize(
            mean=[0.485, 0.456, 0.406],
            std=[0.229, 0.224, 0.225],
        ),
    ])


    def load_frame_as_array(path: Path) -> np.ndarray:
        """Load PNG at path as RGB uint8 (H, W, 3) numpy array."""
        with Image.open(str(path)) as img:
            return np.array(img.convert("RGB"))


    def crop_region(arr: np.ndarray, bbox: tuple[int, int, int, int]) -> np.ndarray:
        """Crop (H, W, 3) array to bbox (x0, y0, x1, y1). Clamps to array bounds."""
        h, w = arr.shape[:2]
        x0, y0, x1, y1 = bbox
        x0 = max(0, min(w - 1, x0))
        x1 = max(x0 + 1, min(w, x1))
        y0 = max(0, min(h - 1, y0))
        y1 = max(y0 + 1, min(h, y1))
        return arr[y0:y1, x0:x1]


    def array_to_tensor(crop: np.ndarray) -> torch.Tensor:
        """Convert (H, W, 3) uint8 crop to (3, 224, 224) preprocessed tensor."""
        if crop.size == 0 or crop.shape[0] < 2 or crop.shape[1] < 2:
            # Degenerate crop — pad to 10x10 black so Resize has something to consume
            crop = np.zeros((10, 10, 3), dtype=np.uint8)
        img = Image.fromarray(crop)
        return _PREPROCESS(img)
    ```

    Create `src/skyherd/vision/detector.py`:

    Imports:
    - `from __future__ import annotations`
    - `from typing import TYPE_CHECKING`
    - `if TYPE_CHECKING: from skyherd.world.cattle import Cow`

    Module constants:
    ```
    _FRAME_W = 640
    _FRAME_H = 480
    _R_X = 18
    _R_Y = 12
    _PAD = 4
    ```

    Functions (signatures + bodies):
    ```
    def cow_bbox_in_frame(cow: "Cow", bounds_m: tuple[float, float]) -> tuple[int, int, int, int]:
        """Reverse-project cow world pos to pixel bbox covering body + head region.

        Formulas match `src/skyherd/vision/renderer.py::_world_to_frame` +
        `_draw_cow_blob` (lines 39-98) byte-for-byte.
        """
        fx = int(cow.pos[0] / bounds_m[0] * _FRAME_W)
        fy = int((1.0 - cow.pos[1] / bounds_m[1]) * _FRAME_H)
        fx = max(0, min(_FRAME_W - 1, fx))
        fy = max(0, min(_FRAME_H - 1, fy))
        tilt = cow.lameness_score
        return (
            max(0, fx - _R_X - _PAD),
            max(0, fy - _R_Y - 16 + tilt),
            min(_FRAME_W - 1, fx + _R_X + _PAD),
            min(_FRAME_H - 1, fy + _R_Y + tilt + 2),
        )


    def eye_crop_bbox(cow_bbox: tuple[int, int, int, int], cow: "Cow") -> tuple[int, int, int, int]:
        """Return 48x48 pixel bbox around the eye region within cow_bbox.

        Mirrors `_draw_cow_blob` eye coordinate computation.
        """
        x0, y0, x1, y1 = cow_bbox
        cx = (x0 + x1) // 2
        cy = (y0 + y1) // 2
        hx = cx + _R_X - 4
        hy = cy - _R_Y + cow.lameness_score - 6
        half = 24
        return (
            max(0, hx - half),
            max(0, hy - half),
            min(_FRAME_W - 1, hx + half),
            min(_FRAME_H - 1, hy + half),
        )
    ```

    Do NOT import `Cow` at module top — use `TYPE_CHECKING` guard to avoid a circular import through `skyherd.world`.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; from skyherd.vision import preprocess as pp; import numpy as np, torch; arr = np.random.randint(0, 255, (480, 640, 3), dtype=np.uint8); c = pp.crop_region(arr, (10, 20, 110, 120)); assert c.shape == (100, 100, 3); t = pp.array_to_tensor(c); assert t.shape == (3, 224, 224); print('preprocess OK')" &amp;&amp; uv run python -c "from skyherd.world.cattle import Cow; from skyherd.vision.detector import cow_bbox_in_frame, eye_crop_bbox; c = Cow(id='c1', tag='T', pos=(300.0, 300.0), lameness_score=0); bb = cow_bbox_in_frame(c, (2000.0, 2000.0)); assert len(bb) == 4; assert bb[0] &lt; bb[2] and bb[1] &lt; bb[3]; eb = eye_crop_bbox(bb, c); assert len(eb) == 4; print('detector OK', bb, eb)" &amp;&amp; uv run ruff check src/skyherd/vision/preprocess.py src/skyherd/vision/detector.py</automated>
  </verify>
  <done>
    - `src/skyherd/vision/preprocess.py` exports `load_frame_as_array`, `crop_region`, `array_to_tensor`, `_PREPROCESS`.
    - `src/skyherd/vision/detector.py` exports `cow_bbox_in_frame`, `eye_crop_bbox` with correct formulas.
    - `uv run ruff check src/skyherd/vision/preprocess.py src/skyherd/vision/detector.py` exits 0.
    - Sanity script above prints `preprocess OK` + `detector OK`.
    - Circular-import guard via `TYPE_CHECKING` on `Cow` import.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite src/skyherd/vision/heads/pinkeye.py as pixel-inference head with rule fallback</name>
  <files>src/skyherd/vision/heads/pinkeye.py</files>
  <behavior>
    - `Pinkeye` remains a subclass of `Head` with `name` property returning `"pinkeye"`.
    - `should_evaluate(cow, frame_meta)` gate unchanged: returns `cow.ocular_discharge > 0.4 or "pinkeye" in cow.disease_flags`.
    - `classify(cow, frame_meta)`:
      * If `should_evaluate(...)` is False: return None.
      * Try the pixel path when `frame_meta.get("raw_path")` is a path-like AND the file exists AND PIL can load it. Otherwise fall back to the existing rule severity mapping.
      * Pixel path: load frame via `load_frame_as_array`, compute `cow_bbox_in_frame(cow, bounds_m)` (derive `bounds_m` from the world — see Action below), compute `eye_crop_bbox`, crop, tensorize, forward through cached model, softmax, argmax. If argmax == 0 return None. Otherwise map to severity and return `DetectionResult(..., bbox=(float x0, float y0, float x1, float y1))` using the COW BBOX (not the eye-crop bbox — the dashboard wants the full cow for visibility).
      * Rule fallback: preserve the EXACT severity + confidence + reasoning formulas from the current `pinkeye.py` (lines 34-89) byte-for-byte. bbox stays None on fallback.
    - Model is loaded lazily via `functools.lru_cache(maxsize=1)` private function `_get_model()`. That function:
      * Builds `mobilenet_v3_small(weights=MobileNet_V3_Small_Weights.IMAGENET1K_V1)`.
      * Replaces `model.classifier[3]` with `nn.Linear(in, 4)`.
      * Loads state via `torch.load(str(weights_path), map_location="cpu", weights_only=True)` and `load_state_dict`.
      * Calls `model.eval()`.
      * Calls `torch.set_num_threads(1)` and `torch.use_deterministic_algorithms(mode=True, warn_only=True)` on first use.
      * `weights_path = importlib.resources.files("skyherd.vision._models") / "pinkeye_mbv3s.pth"`.
    - If the weights file is missing or `torch.load` raises, emit a `logger.warning(...)` and fall back to rule behavior. Never crash the pipeline.
    - The reasoning string for the pixel path MUST include `"pinkeye.md"` AND the cow's tag (preserves both existing tests). Example pixel-path reasoning: `f"Pixel classifier (MobileNetV3-Small) on tag {cow.tag}: {class_name} (p={confidence:.2f}). Per skills/cattle-behavior/disease/pinkeye.md Decision rules."`.
    - `bounds_m` derivation: the head needs the world's bounds to reverse-project. Option A (preferred): read from `frame_meta` — add `"bounds_m"` key in Plan 01 is out of scope; instead default to `(2000.0, 2000.0)` matching `tests/vision/conftest.py::_make_terrain` + `worlds/ranch_a.yaml` (which both use 2000x2000). Option B: accept `frame_meta.get("bounds_m", (2000.0, 2000.0))` so downstream callers can override. USE Option B — the default covers every current test + scenario, the override keeps the door open for multi-ranch configs.
  </behavior>
  <action>
    Completely rewrite `src/skyherd/vision/heads/pinkeye.py`. Preserve the module docstring (Decision rules citation). Final shape:

    Imports block:
    ```
    """Pinkeye (IBK — Infectious Bovine Keratoconjunctivitis) detection head.

    Pixel-level inference via MobileNetV3-Small fine-tuned on synthetic frames.
    Falls back to rule-based severity mapping on Cow.ocular_discharge when no
    frame is available — preserves Head ABC 'tolerate missing keys' contract.

    Thresholds + reasoning aligned with skills/cattle-behavior/disease/pinkeye.md.
    """

    from __future__ import annotations

    import functools
    import importlib.resources
    import logging
    from pathlib import Path
    from typing import Any

    import torch
    import torch.nn as nn
    from torchvision.models import MobileNet_V3_Small_Weights, mobilenet_v3_small

    from skyherd.vision.detector import cow_bbox_in_frame, eye_crop_bbox
    from skyherd.vision.heads.base import Head
    from skyherd.vision.preprocess import array_to_tensor, crop_region, load_frame_as_array
    from skyherd.vision.result import DetectionResult, Severity
    from skyherd.world.cattle import Cow

    logger = logging.getLogger(__name__)

    _CLASS_TO_SEVERITY: tuple[Severity | None, ...] = (None, "watch", "log", "escalate")
    # ^^^ index 0 -> None (no detection), 1 -> watch, 2 -> log, 3 -> escalate
    ```

    Model loader (module-level, lru-cached):
    ```
    @functools.lru_cache(maxsize=1)
    def _get_model() -> nn.Module | None:
        """Build architecture, load fine-tuned state_dict. Returns None on failure.

        Called at most once per process.
        """
        try:
            weights_ref = importlib.resources.files("skyherd.vision._models") / "pinkeye_mbv3s.pth"
            with importlib.resources.as_file(weights_ref) as weights_path:
                model = mobilenet_v3_small(weights=MobileNet_V3_Small_Weights.IMAGENET1K_V1)
                model.classifier[3] = nn.Linear(model.classifier[3].in_features, 4)
                state = torch.load(str(weights_path), map_location="cpu", weights_only=True)
                model.load_state_dict(state)
                model.eval()
                torch.set_num_threads(1)
                torch.use_deterministic_algorithms(mode=True, warn_only=True)
                logger.info("Pinkeye pixel classifier loaded from %s", weights_path)
                return model
        except (FileNotFoundError, ImportError, Exception) as exc:  # noqa: BLE001
            logger.warning(
                "Pinkeye pixel model unavailable (%s) — falling back to rule-based classification",
                exc,
            )
            return None
    ```

    Pinkeye class:

    ```
    class Pinkeye(Head):
        """Pixel-level pinkeye detector; rule-based fallback when no frame available."""

        @property
        def name(self) -> str:
            return "pinkeye"

        def should_evaluate(self, cow: Cow, frame_meta: dict[str, Any]) -> bool:  # noqa: ARG002
            return cow.ocular_discharge > 0.4 or "pinkeye" in cow.disease_flags

        def classify(self, cow: Cow, frame_meta: dict[str, Any]) -> DetectionResult | None:
            if not self.should_evaluate(cow, frame_meta):
                return None

            raw_path = frame_meta.get("raw_path")
            bounds_m = frame_meta.get("bounds_m", (2000.0, 2000.0))

            # Attempt pixel path
            if raw_path is not None:
                raw_path = Path(raw_path)
                if raw_path.exists():
                    pixel_result = self._classify_pixel(cow, raw_path, bounds_m)
                    if pixel_result is not None:
                        return pixel_result
                    # _classify_pixel returned None:
                    # - index 0 (healthy) from model -> genuine no-detection; return None
                    # - model unavailable -> fall through to rule fallback
                    if _get_model() is not None:
                        return None

            # Rule fallback (no frame OR model unavailable)
            return self._classify_rule(cow)

        def _classify_pixel(
            self, cow: Cow, raw_path: Path, bounds_m: tuple[float, float]
        ) -> DetectionResult | None:
            model = _get_model()
            if model is None:
                return None
            try:
                arr = load_frame_as_array(raw_path)
                cow_bbox = cow_bbox_in_frame(cow, bounds_m)
                eye_bbox = eye_crop_bbox(cow_bbox, cow)
                crop = crop_region(arr, eye_bbox)
                tensor = array_to_tensor(crop).unsqueeze(0)
                with torch.no_grad():
                    logits = model(tensor)
                    probs = torch.softmax(logits, dim=1)[0]
                    severity_idx = int(probs.argmax().item())
                    confidence = float(probs[severity_idx].item())
            except Exception as exc:  # noqa: BLE001
                logger.warning("Pinkeye pixel inference failed on %s: %s", cow.tag, exc)
                return None

            severity = _CLASS_TO_SEVERITY[severity_idx]
            if severity is None:
                # Model says healthy — actually a None-detection result; caller handles
                return None

            class_names = ("healthy", "watch", "log", "escalate")
            reasoning = (
                f"Pixel classifier (MobileNetV3-Small) on tag {cow.tag}: "
                f"{class_names[severity_idx]} (p={confidence:.2f}). "
                "Per `skills/cattle-behavior/disease/pinkeye.md` §Decision rules."
            )
            return DetectionResult(
                head_name=self.name,
                cow_tag=cow.tag,
                confidence=round(confidence, 2),
                severity=severity,
                reasoning=reasoning,
                bbox=(
                    float(cow_bbox[0]),
                    float(cow_bbox[1]),
                    float(cow_bbox[2]),
                    float(cow_bbox[3]),
                ),
            )

        def _classify_rule(self, cow: Cow) -> DetectionResult | None:
            """Original rule-based severity mapping — preserved byte-for-byte from pre-Phase-2."""
            discharge = cow.ocular_discharge
            has_flag = "pinkeye" in cow.disease_flags

            if discharge <= 0.4 and not has_flag:
                return None

            severity: Severity
            confidence: float
            reasoning: str

            if has_flag and discharge <= 0.4:
                severity = "log"
                confidence = 0.70
                reasoning = (
                    f"Disease flag 'pinkeye' set on tag {cow.tag}; ocular discharge score "
                    f"{discharge:.2f} is below visual threshold but flag overrides to Tier 2 log. "
                    "Recommend antibiotic evaluation per "
                    "`skills/cattle-behavior/disease/pinkeye.md` §Decision rules."
                )
            elif discharge < 0.6:
                severity = "watch"
                confidence = round(0.5 + (discharge - 0.4) * 2.5, 2)
                reasoning = (
                    f"Ocular discharge score {discharge:.2f}/1.0 on tag {cow.tag} — consistent "
                    "with unilateral tearing (epiphora) without confirmed opacity. "
                    "Tier 1 watch: recheck in 48 hrs per "
                    "`skills/cattle-behavior/disease/pinkeye.md` §Decision rules."
                )
            elif discharge < 0.8:
                severity = "log"
                confidence = round(0.65 + (discharge - 0.6) * 1.5, 2)
                reasoning = (
                    f"Ocular discharge score {discharge:.2f}/1.0 on tag {cow.tag} — "
                    "central corneal opacity likely; dark facial staining and blepharospasm "
                    "visible. Tier 2: antibiotic treatment within 24 hrs and UV shade per "
                    "`skills/cattle-behavior/disease/pinkeye.md` §Decision rules."
                )
            else:
                severity = "escalate"
                confidence = round(min(1.0, 0.80 + (discharge - 0.8) * 1.0), 2)
                reasoning = (
                    f"Ocular discharge score {discharge:.2f}/1.0 on tag {cow.tag} — "
                    "bilateral opacity or deep corneal ulcer suspected; blindness risk. "
                    "Tier 3: rancher call and vet evaluation recommended per "
                    "`skills/cattle-behavior/disease/pinkeye.md` §Decision rules."
                )

            return DetectionResult(
                head_name=self.name,
                cow_tag=cow.tag,
                confidence=confidence,
                severity=severity,
                reasoning=reasoning,
                # bbox left as None — rule fallback has no pixel information
            )
    ```

    IMPORTANT subtlety: when the pixel model classifies a cow as index 0 (healthy) despite the `should_evaluate` gate firing, `classify` MUST return None (real no-detection). Only fall back to rule when the MODEL IS UNAVAILABLE (not when the model gives a healthy prediction). The logic above implements this: `_classify_pixel` returns None on both "model unavailable" and "healthy prediction"; the outer `classify` distinguishes by checking `_get_model() is not None` to decide whether to rule-fall-back.

    Do NOT modify `heads/__init__.py` or `registry.py` — they already export/register `Pinkeye` by class name.

    Do NOT delete `_classify_rule`'s docstring or any reasoning strings that contain `pinkeye.md` — those are test-asserted.
  </action>
  <verify>
    <automated>uv run pytest tests/vision/test_heads/test_pinkeye.py -x &amp;&amp; uv run ruff check src/skyherd/vision/heads/pinkeye.py &amp;&amp; uv run python -c "from skyherd.vision.heads.pinkeye import Pinkeye, _get_model; m = _get_model(); assert m is not None, 'model failed to load — Plan 03 weights missing or corrupt'; print('model loaded, params=', sum(p.numel() for p in m.parameters()))" &amp;&amp; uv run python -c "from skyherd.vision.heads.pinkeye import Pinkeye; from skyherd.world.cattle import Cow; p = Pinkeye(); c = Cow(id='c1', tag='T', pos=(300.0, 300.0), ocular_discharge=0.85, disease_flags={'pinkeye'}); r = p.classify(c, {}); assert r is not None, 'rule fallback did not fire without raw_path'; assert 'pinkeye.md' in r.reasoning; assert 'T' in r.reasoning; assert r.bbox is None, 'rule fallback must leave bbox=None'; print('rule fallback OK:', r.severity)"</automated>
  </verify>
  <done>
    - `uv run pytest tests/vision/test_heads/test_pinkeye.py -x` PASSES all 9 existing tests (rule fallback fires for `frame_meta={}` — existing tests all pass raw_path-less meta).
    - `_get_model()` loads successfully (Plan 03 weights are in place).
    - `grep -c "mobilenet_v3_small" src/skyherd/vision/heads/pinkeye.py` returns >= 1.
    - `grep -c "pinkeye.md" src/skyherd/vision/heads/pinkeye.py` returns >= 5 (preserved in both rule + pixel reasoning strings).
    - `grep -c "bbox=" src/skyherd/vision/heads/pinkeye.py` returns >= 1 (pixel path sets bbox).
    - `uv run ruff check src/skyherd/vision/heads/pinkeye.py` exits 0.
    - All 6 other head test files still PASS (`uv run pytest tests/vision/test_heads/ -x`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frame_meta["raw_path"] -> Image.open | Caller-provided path; potential path traversal |
| src/skyherd/vision/_models/pinkeye_mbv3s.pth -> torch.load | Pickled weights; potential code execution via malicious .pth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | T (Tampering) | torch.load on pinkeye_mbv3s.pth | mitigate | Use `weights_only=True` (torch 2.4+). Enforced in `_get_model`. |
| T-02-09 | T (Tampering) | Path traversal via frame_meta["raw_path"] | mitigate | `Path(raw_path).exists()` check before load; PIL Image open is sandboxed to PNG bytes. Wave 5 can add allow-list check if needed. |
| T-02-10 | D (DoS) | Decompression bomb PNG | accept | PIL default `MAX_IMAGE_PIXELS` cap applies; rendered frames are 640x480 PNGs from trusted renderer; Phase 5 may tighten at HTTP boundary. |
| T-02-11 | I (Information disclosure) | Logger warnings leak weight paths | accept | Paths are inside the package directory — no user data leaked. |
| T-02-12 | S (Spoofing) | Swapped .pth silently mislabels cows | mitigate | WEIGHTS.md SHA-256 documented by Plan 03; Plan 05 adds a runtime `test_pinkeye_bbox_for_sick_cow` integration test that would catch label-swap regressions. |
</threat_model>

<verification>
```
uv run pytest tests/vision/test_heads/test_pinkeye.py -x
uv run pytest tests/vision/ -x
uv run pytest --cov=src/skyherd/vision --cov-report=term-missing tests/vision/
make demo SEED=42 SCENARIO=sick_cow
```

Determinism check: run the sick_cow scenario twice back-to-back, diff SSE-stream hashes after wall-time sanitization. Must match.
</verification>

<success_criteria>
- [ ] `src/skyherd/vision/preprocess.py` exists, exports 3 functions + `_PREPROCESS`.
- [ ] `src/skyherd/vision/detector.py` exists, exports `cow_bbox_in_frame` + `eye_crop_bbox`.
- [ ] `src/skyherd/vision/heads/pinkeye.py` is rewritten: imports torchvision, uses lru-cached model load, has pixel path + rule fallback, bbox populated on pixel path.
- [ ] All 9 existing `test_pinkeye.py` tests PASS unchanged (rule fallback fires for `frame_meta={}`).
- [ ] `_get_model()` loads the Plan 03 weights without errors.
- [ ] Other 6 head test files still PASS (no regression on rule heads).
- [ ] `uv run ruff check src/skyherd/vision/{heads/pinkeye.py,preprocess.py,detector.py}` exits 0.
- [ ] Sick_cow scenario still PASSES `make demo SEED=42 SCENARIO=sick_cow`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vision-pixel-inference/02-04-SUMMARY.md` with:
- Confirmation that pixel + rule paths both work.
- Any surprises re: Pydantic model_dump on DetectionResult with a bbox tuple (Pydantic v2 may emit a list rather than tuple in .model_dump — document if encountered).
- Mean inference time measured on a single frame (stopwatch in SUMMARY body) — NOT yet budget-asserted (Plan 05 does that).
- Whether the cached model is truly loaded once per process (lru_cache hit count).
</output>
