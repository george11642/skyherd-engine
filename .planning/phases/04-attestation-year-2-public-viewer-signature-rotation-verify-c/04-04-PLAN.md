---
phase: 4
plan: 04-04
type: docs
autonomous: true
requirements: [ATT-05]
---

# Plan 04-04 — Docs + E2E Integration Test

## Objective

Ship the end-to-end walkthrough documentation and a single integration test proving all four preceding plans compose correctly (init → append → rotate → append → verify both halves → run `skyherd-verify` CLI).

## Tasks

1. **[auto]** Write `docs/ATTESTATION.md` — the hero doc. Sections:
   - What is the SkyHerd attestation chain
   - Genesis + hash-chain mechanics
   - Ed25519 signing contract
   - Using `skyherd-attest init | append | verify | list`
   - Using `skyherd-verify verify-event | verify-chain`
   - The public viewer (`/attest/:hash`)
   - Memory version pairing (two-receipts-agree)
   - Signature rotation: when + how (link to `ATTESTATION_ROTATION.md`)
   All commands must be copy-pasteable.
2. **[auto]** Write `docs/ATTESTATION_ROTATION.md` — operational runbook:
   - When to rotate (quarterly, after compromise, after staff departure)
   - Exact commands: archive old key, generate new, confirm old entries still verify
   - How post-rotation entries still chain from pre-rotation last hash
   - Recovery path if an old archived key is lost (old entries become unverifiable for sig, hash linkage remains — document this honestly)
3. **[auto]** Add `tests/attest/test_integration_rotation.py`:
   - Create signer A → ledger → append 3 events (with memver_ids) → rotate to signer B → append 3 more events → `ledger.verify()` returns valid=True, total=6 → `skyherd-verify verify-chain` CLI on same DB exits 0.
4. **[auto]** Update `CLAUDE.md` "Reading order" to mention `docs/ATTESTATION.md` as judge-facing.

## Verification

- `uv run pytest tests/attest/test_integration_rotation.py -q`
- `make test` — full suite green.
- Manual spot-check: `less docs/ATTESTATION.md` — copy each command block, run in `/tmp/`, all exit 0.

## Done criteria

- [ ] `docs/ATTESTATION.md` + `docs/ATTESTATION_ROTATION.md` created.
- [ ] Integration test passes.
- [ ] CLAUDE.md reading order updated.
