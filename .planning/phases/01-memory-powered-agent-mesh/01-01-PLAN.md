---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skyherd/agents/memory_paths.py
  - tests/agents/test_memory_paths.py
  - web/src/components/shared/HashChip.tsx
  - web/src/components/shared/HashChip.test.tsx
  - tests/test_determinism_e2e.py
  - tests/agents/test_memory.py
  - tests/agents/test_memory_hook.py
  - tests/agents/test_memory_determinism.py
  - tests/server/test_memory_api.py
  - web/src/components/MemoryPanel.test.tsx
  - scripts/a1_probe.py
  - docs/A1_PROBE_RESULT.md
autonomous: true
requirements: [MEM-09]
user_setup: []

must_haves:
  truths:
    - "A1 (extra_body memory_store attach) status is documented with live-API evidence OR scoped fallback"
    - "HashChip is a standalone shared component consumed by AttestationPanel with zero visual/test regression"
    - "memory_paths.decide_write_path returns deterministic (path, content) tuples with no datetime.now()"
    - "Determinism sanitizer strips memver_/mem_/memstore_ IDs"
    - "Wave 0 test stubs exist for every downstream plan (pytest collects them; they fail with unimplemented imports, NOT syntax errors)"
  artifacts:
    - path: "src/skyherd/agents/memory_paths.py"
      provides: "pure-function path+content decider per-agent"
      contains: "def decide_write_path"
    - path: "web/src/components/shared/HashChip.tsx"
      provides: "extracted 4-swatch HashChip with click-to-copy"
      exports: ["HashChip"]
    - path: "tests/test_determinism_e2e.py"
      provides: "sanitizer extended for memver_/mem_/memstore_ IDs"
      contains: "memver_"
    - path: "docs/A1_PROBE_RESULT.md"
      provides: "A1 assumption verification record: PASS/FAIL + response body + chosen path"
    - path: "tests/agents/test_memory.py"
      provides: "Wave 0 stub for Plan 01-02 (pytest imports fail gracefully)"
    - path: "tests/agents/test_memory_hook.py"
      provides: "Wave 0 stub for Plan 01-04"
    - path: "tests/server/test_memory_api.py"
      provides: "Wave 0 stub for Plan 01-05"
    - path: "web/src/components/MemoryPanel.test.tsx"
      provides: "Wave 0 stub for Plan 01-06"
  key_links:
    - from: "AttestationPanel.tsx"
      to: "web/src/components/shared/HashChip.tsx"
      via: "import { HashChip } from '@/components/shared/HashChip'"
      pattern: "from [\"']@/components/shared/HashChip[\"']"
    - from: "tests/test_determinism_e2e.py"
      to: "DETERMINISM_SANITIZE"
      via: "regex entry"
      pattern: "memver_"
---

<objective>
Wave 0 foundation plan: validate the A1 assumption live, extract HashChip into a shared component, scaffold the deterministic `memory_paths.py` module, extend the determinism sanitizer to strip Memory IDs, and produce Wave 0 test stubs for every downstream plan so Nyquist sampling is green from Task 01-01 onward.

Purpose: Kill-switch any plan that builds on A1 before we know it holds. Establish the shared `HashChip` so Plan 01-06 (MemoryPanel) can import cleanly. Preserve determinism by extending the sanitizer ahead of any Memory ID appearing in demo output.
Output: One passing live-API probe (or documented fallback), a shared HashChip import path, a pure `memory_paths.py`, extended sanitizer regex, and 5 test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-memory-powered-agent-mesh/01-CONTEXT.md
@.planning/phases/01-memory-powered-agent-mesh/01-RESEARCH.md
@.planning/phases/01-memory-powered-agent-mesh/01-PATTERNS.md
@web/src/components/AttestationPanel.tsx
@web/src/components/AttestationPanel.test.tsx
@tests/test_determinism_e2e.py
@src/skyherd/agents/spec.py

<interfaces>
<!-- Canonical REST shapes from CONTEXT.md `<spike_findings>` — every downstream plan parses to these. -->

```python
# Memory REST response shape (confirmed live 2026-04-23 20:10 UTC)
MemoryStore  = {id: "memstore_<base62>", name, description, type: "memory_store", created_at, updated_at, archived_at?}
Memory       = {id: "mem_<base62>", memory_version_id: "memver_<base62>",
                content_sha256, content_size_bytes, path, created_at, updated_at}
MemoryVersion = {id: "memver_<base62>", operation: "created"|"updated"|"deleted"|"redacted",
                 created_by: {type: "api_actor", api_key_id: "apikey_..."},
                 path, content_sha256?, content_size_bytes?, redacted_by?}
ListEnvelope = {data: [...], prefixes?: [...]}  # returned by list_memories
```

```python
# Pure-function signature (Plan 01-01 writes the impl; 01-04 uses it)
def decide_write_path(agent_name: str, event: dict[str, Any], tool_calls: list[dict[str, Any]]) -> tuple[str, str]:
    """Return (memory_path, markdown_content). NO datetime.now(). NO I/O."""
```

```typescript
// Shared component (Plan 01-01 extracts it; 01-06 reuses)
export interface HashChipProps { hash: string; label?: string; }
export function HashChip(props: HashChipProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A1 live probe — verify extra_body memory_store attach works end-to-end</name>
  <files>scripts/a1_probe.py, docs/A1_PROBE_RESULT.md</files>
  <read_first>
    - .planning/phases/01-memory-powered-agent-mesh/01-CONTEXT.md (§spike_findings lines 114-156 — confirmed REST surface)
    - .planning/phases/01-memory-powered-agent-mesh/01-RESEARCH.md (§Pattern 2 lines 424-468, §Assumptions Log lines 955-966 A1 row, §Open Questions §1 lines 970-974)
    - src/skyherd/agents/managed.py (lines 235-262 — create_session_async, the target modification site)
  </read_first>
  <action>
    Create `scripts/a1_probe.py` — a throwaway self-cleaning script that exercises A1. Exact steps:

    1. If `ANTHROPIC_API_KEY` is unset: exit 0 with stdout `A1_PROBE_SKIPPED: no ANTHROPIC_API_KEY set — A1 remains ASSUMED` and write `docs/A1_PROBE_RESULT.md` with `status: SKIPPED` + fallback plan text. DO NOT fail the task.
    2. If key set: instantiate `anthropic.AsyncAnthropic(api_key=os.environ["ANTHROPIC_API_KEY"])`. Use `client.post` with headers `{"anthropic-beta": "managed-agents-2026-04-01"}` to:
       a. POST `/v1/memory_stores` body `{"name":"a1_probe_<8hex>", "description":"A1 assumption probe — archive immediately"}` — capture `id` (must start `memstore_`).
       b. POST `/v1/memory_stores/{id}/memories` body `{"path":"a1/probe.md","content":"A1 probe content"}` — confirm `memory_version_id` starts `memver_`.
       c. POST `/v1/agents` body `{"name":"a1_probe_agent","model":"claude-sonnet-4-5"}` capture `id`.
       d. POST `/v1/environments` body `{"name":"a1_probe_env"}` capture `id`.
       e. Call `client.beta.sessions.create(agent=agent_id, environment_id=env_id, title="a1_probe", extra_body={"resources":[{"type":"memory_store","memory_store_id":"<memstore_id>","mode":"read_write"}]})`. Capture the FULL response.
       f. Cleanup: POST `/v1/memory_stores/{id}/archive` body `{}`; archive the agent + environment if endpoints exist (best-effort try/except — do NOT fail if cleanup endpoints 404).
    3. Branch on outcome:
       - **PASS** — response is 2xx and response body shows the memory_store attachment (either echoed in `resources` or referenced in session config): write `docs/A1_PROBE_RESULT.md` with `status: PASS`, the full sanitized response body (redact workspace/api_key_id), and stdout `A1_PROBE_PASS`. Exit 0.
       - **REJECT** — 4xx with message mentioning `resources` / `memory_store` / `type` not recognized: write `status: FAIL (extra_body rejected)` + fallback directive: `"Plan 01-03 MUST switch to raw client.post('/v1/sessions', body={agent, environment_id, title, resources:[...]})"` — and stdout `A1_PROBE_FAIL_USE_RAW_POST`. Exit 0 (planner decides).
       - **IGNORED** — 2xx but no memory_store echoed in response: write `status: UNCLEAR — attach accepted but no confirmation echoed` + directive: `"Plan 01-04 MUST verify attach via listing events / session config retrieval"`. Exit 0.

    Do NOT wrap in asyncio.run at module level — use `if __name__ == "__main__": asyncio.run(main())` pattern. Redact `api_key_id` and any `apikey_` substring from the written doc via regex `r"apikey_[A-Za-z0-9]+"` → `"apikey_REDACTED"`.
  </action>
  <acceptance_criteria>
    - File `scripts/a1_probe.py` exists.
    - `uv run python scripts/a1_probe.py` exits 0 and prints one of `A1_PROBE_PASS`, `A1_PROBE_SKIPPED`, `A1_PROBE_FAIL_USE_RAW_POST`, or `A1_PROBE_UNCLEAR`.
    - File `docs/A1_PROBE_RESULT.md` exists and contains a `status:` line with one of the four values.
    - `grep -E "apikey_[A-Za-z0-9]+" docs/A1_PROBE_RESULT.md` returns no unredacted matches (only `apikey_REDACTED`).
    - No spike stores remain — the probe calls `archive_store` in a `finally` block.
  </acceptance_criteria>
  <verify>
    <automated>uv run python scripts/a1_probe.py &amp;&amp; test -f docs/A1_PROBE_RESULT.md &amp;&amp; grep -qE "^status:" docs/A1_PROBE_RESULT.md</automated>
  </verify>
  <done>A1 status is documented in docs/A1_PROBE_RESULT.md with one of: PASS / SKIPPED / FAIL_USE_RAW_POST / UNCLEAR. All spike state archived.</done>
</task>

<task type="auto">
  <name>Task 2: Extract HashChip to shared component + wire AttestationPanel to consume it</name>
  <files>web/src/components/shared/HashChip.tsx, web/src/components/shared/HashChip.test.tsx, web/src/components/AttestationPanel.tsx</files>
  <read_first>
    - web/src/components/AttestationPanel.tsx (lines 48-113 — the local HashChip component)
    - web/src/components/AttestationPanel.test.tsx (lines 197-234 — existing HashChip tests that must continue passing)
    - .planning/phases/01-memory-powered-agent-mesh/01-PATTERNS.md (§`web/src/components/MemoryPanel.tsx` section, HashChip reuse note)
  </read_first>
  <action>
    1. Create `web/src/components/shared/HashChip.tsx` exporting `HashChip` with signature `export function HashChip({ hash, label }: { hash: string; label?: string }): JSX.Element`. COPY the exact implementation from `AttestationPanel.tsx` lines 48-113 verbatim — including:
       - The `data-testid="hash-chip"` attribute.
       - The `data-testid="hash-swatch"` attribute (must produce exactly 4 swatches for the existing `swatches.length === 4` test).
       - `navigator.clipboard.writeText` with try/catch fallback.
       - The 4×6-hex-char swatch rendering (derive swatches from sha256 of the hash string if the hash itself is not pure hex — this keeps `memver_01XRSVdKC1McTbhVbVF5T47E` working).
       - Tooltip component import from `@/components/ui/tooltip`.
    2. Modify `AttestationPanel.tsx`:
       - Remove the inline `HashChip` (lines 48-113).
       - Add `import { HashChip } from "@/components/shared/HashChip";` at the top alongside existing imports.
       - Leave every call site calling `<HashChip hash={...} />` unchanged.
    3. Create `web/src/components/shared/HashChip.test.tsx` — minimal vitest suite asserting:
       - `render(<HashChip hash="memver_01XRSVdKC1McTbhVbVF5T47E" />)` produces `[data-testid='hash-chip']` element.
       - Rendered component has exactly 4 `[data-testid='hash-swatch']` elements.
       - Clicking the chip calls `navigator.clipboard.writeText` with the full hash string (use `vi.stubGlobal("navigator", { clipboard: { writeText: vi.fn() }})`).
  </action>
  <acceptance_criteria>
    - `web/src/components/shared/HashChip.tsx` exists and exports `HashChip`.
    - `grep -q "import { HashChip } from \"@/components/shared/HashChip\"" web/src/components/AttestationPanel.tsx` — true.
    - `grep -c "function HashChip" web/src/components/AttestationPanel.tsx` returns 0 (no local definition remains).
    - `(cd web && pnpm vitest run src/components/AttestationPanel.test.tsx)` passes with zero test regressions.
    - `(cd web && pnpm vitest run src/components/shared/HashChip.test.tsx)` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; pnpm vitest run src/components/AttestationPanel.test.tsx src/components/shared/HashChip.test.tsx</automated>
  </verify>
  <done>HashChip lives at `web/src/components/shared/HashChip.tsx`. AttestationPanel imports it. AttestationPanel tests green. New HashChip standalone test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: memory_paths.py pure module + determinism sanitizer extension + Wave 0 test stubs</name>
  <files>src/skyherd/agents/memory_paths.py, tests/agents/test_memory_paths.py, tests/test_determinism_e2e.py, tests/agents/test_memory.py, tests/agents/test_memory_hook.py, tests/agents/test_memory_determinism.py, tests/server/test_memory_api.py, web/src/components/MemoryPanel.test.tsx</files>
  <read_first>
    - .planning/phases/01-memory-powered-agent-mesh/01-RESEARCH.md (§Pattern 3 lines 541-576 — decide_write_path reference impl; §Validation Architecture lines 895-927 — Wave 0 gaps list)
    - .planning/phases/01-memory-powered-agent-mesh/01-PATTERNS.md (§`src/skyherd/agents/memory_paths.py` section + §Determinism guard section)
    - src/skyherd/agents/spec.py (analog — small pure module layout)
    - tests/test_determinism_e2e.py (current DETERMINISM_SANITIZE at lines 21-30)
    - src/skyherd/server/events.py (lines 33-39 — AGENT_NAMES constant)
  </read_first>
  <behavior>
    - Test: decide_write_path("PredatorPatternLearner", {"classification":"coyote.confirmed","ranch_id":"ranch_a","type":"predator.detected"}, []) returns ("patterns/coyote-crossings.md", content containing "coyote").
    - Test: decide_write_path("HerdHealthWatcher", {"tag":"T042","type":"cow.motion"}, []) returns ("notes/T042.md", non-empty).
    - Test: decide_write_path("CalvingWatch", {"tag":"T042","type":"labor.signal"}, []) returns ("baselines/T042.md", non-empty).
    - Test: decide_write_path("FenceLineDispatcher", {"segment":"seg_1","type":"fence.breach"}, []) returns ("notes/dispatch-seg_1.md", non-empty).
    - Test: decide_write_path("GrazingOptimizer", {"ranch_id":"ranch_a","type":"grazing.weekly"}, []) returns ("notes/rotation-proposal.md", non-empty).
    - Test: `_redact({"rancher_phone":"+15551234567","type":"evt"})` returns `{"rancher_phone":"[REDACTED]","type":"evt"}`.
    - Test: decide_write_path() result is byte-identical across two back-to-back calls with identical args (no datetime.now, no uuid).
    - Test (determinism): DETERMINISM_SANITIZE list contains regex entries matching `memver_0123ABCDEF`, `mem_0123ABCDEF`, `memstore_0123ABCDEF`.
  </behavior>
  <action>
    **Part A — Create `src/skyherd/agents/memory_paths.py`** (~80 lines). Copy RESEARCH.md §Pattern 3 lines 541-576 verbatim as the starting shape, then:
    - Imports: `from __future__ import annotations; from typing import Any`. NO `datetime`, NO `uuid`, NO `random`, NO `time`.
    - Module-level constant `_REDACT_KEYS = frozenset({"rancher_phone", "vet_phone", "auth_token", "api_key", "twilio_sid"})`.
    - `def _redact(d: dict[str, Any]) -> dict[str, Any]:` returns new dict with values replaced by `"[REDACTED]"` for redact keys. IMMUTABLE — never mutate input.
    - `def decide_write_path(agent_name: str, event: dict[str, Any], tool_calls: list[dict[str, Any]]) -> tuple[str, str]:` — branches per agent name from `AGENT_NAMES` at `src/skyherd/server/events.py:33-39`. For unknown agent names, raise `ValueError(f"unknown agent: {agent_name!r}")`.
    - Content MUST NOT include any wall-clock timestamp. If a timestamp is needed, pull `ts` from `event.get("ts")` (caller's responsibility; fall back to empty string).
    - Module docstring cites source: `# Reference: .planning/phases/01-memory-powered-agent-mesh/01-RESEARCH.md §Pattern 3`.

    **Part B — Create `tests/agents/test_memory_paths.py`** — covering every behavior listed above. Use `@pytest.mark.asyncio` only where needed (this module is sync, so pure `def test_*`).

    **Part C — Extend `tests/test_determinism_e2e.py` DETERMINISM_SANITIZE** at lines 21-30. Append these entries (before the closing `]`):
    ```python
    # Memory IDs — sanitize when SKYHERD_AGENTS=managed is used in replay
    (r"memver_[A-Za-z0-9]+", "memver_XXXXXX"),
    (r"\bmem_[A-Za-z0-9]+", "mem_XXXXXX"),
    (r"memstore_[A-Za-z0-9]+", "memstore_XXXXXX"),
    ```
    Leave the existing entries untouched.

    **Part D — Create the 5 Wave 0 test stubs.** These stubs exist so Nyquist sampling has green files in Wave 0, and downstream plans (01-02, 01-04, 01-05, 01-06) just add real test functions to them.

    1. `tests/agents/test_memory.py` — content:
       ```python
       """Wave 0 stub — tests appended by Plan 01-02."""
       import pytest

       @pytest.mark.skip(reason="Plan 01-02 not yet implemented")
       def test_memory_manager_exists_stub() -> None:
           """Placeholder; real tests live in Plan 01-02."""
           pass
       ```
    2. `tests/agents/test_memory_hook.py` — same shape, skip reason `"Plan 01-04 not yet implemented"`.
    3. `tests/agents/test_memory_determinism.py` — same shape, skip reason `"Plan 01-07 not yet implemented"`.
    4. `tests/server/test_memory_api.py` — same shape, skip reason `"Plan 01-05 not yet implemented"`.
    5. `web/src/components/MemoryPanel.test.tsx` — content:
       ```typescript
       // Wave 0 stub — tests appended by Plan 01-06.
       import { describe, it } from "vitest";
       describe.skip("MemoryPanel (Plan 01-06 not yet implemented)", () => {
         it("placeholder", () => {});
       });
       ```
  </action>
  <acceptance_criteria>
    - `src/skyherd/agents/memory_paths.py` exists and exports `decide_write_path`.
    - `grep -cE "datetime|uuid\\.|random|time\\." src/skyherd/agents/memory_paths.py` returns 0 (no non-deterministic imports).
    - `uv run pytest tests/agents/test_memory_paths.py -x` passes (≥7 tests).
    - `grep -q "memver_" tests/test_determinism_e2e.py &amp;&amp; grep -q "memstore_" tests/test_determinism_e2e.py` — true.
    - `uv run pytest tests/test_determinism_e2e.py::test_demo_seed42_is_deterministic_3x -x --no-header` — passes (existing test still green; sanitizer additions are harmless no-ops when no Memory IDs present).
    - All 5 stub files exist; `uv run pytest tests/agents/test_memory.py tests/agents/test_memory_hook.py tests/agents/test_memory_determinism.py tests/server/test_memory_api.py --collect-only -q` lists skipped tests with no collection errors.
    - `(cd web &amp;&amp; pnpm vitest run src/components/MemoryPanel.test.tsx)` exits 0 (all skipped).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agents/test_memory_paths.py tests/test_determinism_e2e.py -x &amp;&amp; cd web &amp;&amp; pnpm vitest run src/components/MemoryPanel.test.tsx</automated>
  </verify>
  <done>memory_paths.py pure module passes ≥7 tests. Determinism sanitizer extended. 5 Wave 0 stubs exist and collect cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→Anthropic API (probe only) | A1 probe hits live API with ANTHROPIC_API_KEY |
| test harness→filesystem | pure-function module; no FS writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `docs/A1_PROBE_RESULT.md` | mitigate | Redact `apikey_[A-Za-z0-9]+` via regex before writing; store under `docs/` (already gitignored for secrets? No — verify `.gitignore` covers nothing sensitive ended up in doc) |
| T-01-02 | Information Disclosure | `scripts/a1_probe.py` stdout | mitigate | Never echo raw API response; print only the status token |
| T-01-03 | Integrity | `tests/test_determinism_e2e.py` sanitizer regex | mitigate | Append — never replace existing entries; test that existing determinism suite remains green |
| T-01-04 | Tampering | A1 probe spike state leakage | mitigate | `finally: await archive_store(...)` in probe; probe name includes 8-hex random suffix so collision impossible |
</threat_model>

<verification>
- `uv run pytest tests/agents/test_memory_paths.py tests/test_determinism_e2e.py -x` passes.
- `(cd web && pnpm vitest run src/components/shared/HashChip.test.tsx src/components/AttestationPanel.test.tsx src/components/MemoryPanel.test.tsx)` passes.
- `docs/A1_PROBE_RESULT.md` contains a status line.
- No `datetime.now`, `uuid`, `random`, or `time.time` in `src/skyherd/agents/memory_paths.py`.
</verification>

<success_criteria>
- A1 assumption status recorded (PASS / FAIL / SKIPPED / UNCLEAR).
- HashChip extracted; AttestationPanel consumes shared component; all pre-existing tests green.
- memory_paths.py pure module passes tests and has zero non-deterministic imports.
- Determinism sanitizer extended with memver_/mem_/memstore_ regex entries.
- 5 Wave 0 test stubs exist and collect cleanly (pytest + vitest).
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-powered-agent-mesh/01-01-SUMMARY.md` recording:
- A1 status (copy from docs/A1_PROBE_RESULT.md `status:` line).
- Any downstream plan adjustments needed (if A1 FAILED — which plans need the raw-POST fallback).
- HashChip import path confirmation.
- Links to modified files.
</output>
