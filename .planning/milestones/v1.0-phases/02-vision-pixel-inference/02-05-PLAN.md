---
phase: 02-vision-pixel-inference
plan: 05
type: execute
wave: 4
depends_on: [02-02, 02-04]
files_modified:
  - tests/vision/test_heads/test_pinkeye.py
  - tests/vision/test_heads/test_pinkeye_pixel.py
  - tests/scenarios/test_sick_cow.py
autonomous: true
requirements: [VIS-01, VIS-03, VIS-04, VIS-05]
tags: [vision, tests, scenarios, latency, determinism]
must_haves:
  truths:
    - "All 9 pre-existing rule-path tests in `tests/vision/test_heads/test_pinkeye.py` continue to PASS (rule fallback preserves every severity/confidence/reasoning assertion byte-for-byte)"
    - "`tests/vision/test_heads/test_pinkeye_pixel.py` proves: positive frame -> non-None detection with bbox set; negative frame -> None; two back-to-back calls produce equal results (determinism)"
    - "A latency test asserts single-frame pinkeye pixel inference completes in under 500ms on the dev-box CPU baseline (median of 5 runs)"
    - "The sick_cow scenario test asserts at least one DetectionResult in the SSE stream has `head_name='pinkeye'` AND `bbox is not None` AND bbox is within frame bounds (0 <= x0 < x1 <= 640, 0 <= y0 < y1 <= 480)"
    - "`test_reasoning_contains_skill_reference` (preserves `pinkeye.md` citation) passes on BOTH rule + pixel paths"
    - "VALIDATION.md Per-Task Map is filled with all Wave 0-4 tasks, each with an automated verify command"
  artifacts:
    - path: "tests/vision/test_heads/test_pinkeye.py"
      provides: "Rule-path tests + explicit rule-fallback coverage (updated, not replaced)"
      contains: "test_reasoning_contains_skill_reference"
    - path: "tests/vision/test_heads/test_pinkeye_pixel.py"
      provides: "Pixel-path tests: positive/negative frame, determinism, latency, bbox shape"
      contains: "test_inference_under_500ms_cpu"
      min_lines: 100
    - path: "tests/scenarios/test_sick_cow.py"
      provides: "Sick_cow scenario now asserts a pinkeye detection with real bbox in SSE payload"
      contains: "bbox"
  key_links:
    - from: "tests/vision/test_heads/test_pinkeye_pixel.py"
      to: "src/skyherd/vision/heads/pinkeye.py (pixel path)"
      via: "rendered_positive_frame / rendered_negative_frame fixtures"
      pattern: "rendered_positive_frame|rendered_negative_frame"
    - from: "tests/scenarios/test_sick_cow.py"
      to: "ClassifyPipeline (via scenario runtime)"
      via: "SSE detection payload bbox field"
      pattern: "bbox"
---

<objective>
Close the Phase 2 test + validation gate: prove every pinkeye behavior (rule fallback, pixel inference, latency, determinism, sick_cow scenario bbox) works end-to-end, AND fill the VALIDATION.md Per-Task Map so the Nyquist sampling contract is met.

Purpose: Plans 01-04 delivered contracts, weights, and pixel head. This plan is the verification harness that makes the phase landable: it preserves the 9 original rule-path tests (zero regression), adds a dedicated pixel-path test file that exercises the new inference path against `rendered_positive_frame` / `rendered_negative_frame` fixtures from Plan 02, proves the <500ms/frame CPU budget via a benchmark test, and extends the sick_cow scenario to assert a real pinkeye bbox reaches the SSE payload. Finally it fills out VALIDATION.md's Per-Task Map so the phase validation contract is signed off.

Output: Updated `test_pinkeye.py` (explicit rule fallback, no behavior change), new `test_pinkeye_pixel.py` (pixel path), extended `test_sick_cow.py` (scenario bbox assertion), fully-populated `02-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-vision-pixel-inference/02-RESEARCH.md
@.planning/phases/02-vision-pixel-inference/02-PATTERNS.md
@.planning/phases/02-vision-pixel-inference/02-VALIDATION.md
@.planning/phases/02-vision-pixel-inference/02-01-SUMMARY.md
@.planning/phases/02-vision-pixel-inference/02-02-SUMMARY.md
@.planning/phases/02-vision-pixel-inference/02-03-SUMMARY.md
@.planning/phases/02-vision-pixel-inference/02-04-SUMMARY.md
@src/skyherd/vision/heads/pinkeye.py
@src/skyherd/vision/pipeline.py
@tests/vision/conftest.py
@tests/vision/test_heads/test_pinkeye.py
@tests/vision/test_pipeline.py
@tests/scenarios/test_sick_cow.py

<interfaces>
Fixtures available from Plan 02 (in `tests/vision/conftest.py`):
- `sick_pinkeye_world` -> World with 1 cow SICK01 at (300, 300), discharge=0.85, flag="pinkeye"
- `rendered_positive_frame` -> (png_path, world); the world has a visibly-sick cow
- `rendered_negative_frame` -> (png_path, world); all healthy cows

Pinkeye contract from Plan 04:
```
Pinkeye().classify(cow, frame_meta) -> DetectionResult | None
# frame_meta == {} -> rule path (bbox=None)
# frame_meta == {"raw_path": png_path} -> pixel path (bbox set on non-None)
# bounds_m defaults to (2000.0, 2000.0) — conftest terrain matches this
```

Scenario infrastructure hint (from `src/skyherd/scenarios/`): sick_cow scenario is invoked via `make demo SEED=42 SCENARIO=sick_cow` and its test lives in `tests/scenarios/test_sick_cow.py`. The test should assert on the detection payload produced by `ClassifyPipeline` during the scenario — check existing file for the exact API.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update tests/vision/test_heads/test_pinkeye.py — keep rule tests passing + add explicit rule-fallback coverage</name>
  <files>tests/vision/test_heads/test_pinkeye.py</files>
  <behavior>
    - All 9 existing tests continue to PASS. The executor MUST NOT delete or rename any existing test function; they all run the rule path (frame_meta={}), which still returns the existing severities/confidences/reasonings byte-for-byte.
    - Add 1 new test `test_rule_fallback_fires_when_raw_path_missing` — explicit assertion that with `frame_meta={"raw_path": None}`, classify() falls back to the rule mapping and returns bbox=None.
    - Add 1 new test `test_rule_fallback_fires_when_raw_path_nonexistent` — with `frame_meta={"raw_path": Path("/nonexistent/file.png")}`, classify() falls back and returns bbox=None.
    - `test_reasoning_contains_skill_reference` is PRESERVED (reasoning must contain "pinkeye.md") — already tested via the rule path which still cites pinkeye.md.
    - Do NOT change the `_make_cow` helper's default (pos=(100.0, 100.0) stays fine for rule-path tests — bounds_m is irrelevant when raw_path missing).
  </behavior>
  <action>
    Read `tests/vision/test_heads/test_pinkeye.py` end-to-end. Do NOT modify any of the 9 existing test function bodies. Append two new tests after `test_reasoning_contains_cow_tag`:

    ```
    from pathlib import Path


    def test_rule_fallback_fires_when_raw_path_missing() -> None:
        """frame_meta without raw_path -> rule path -> bbox is None."""
        cow = _make_cow(ocular_discharge=0.75)
        result = _HEAD.classify(cow, {"raw_path": None})
        assert result is not None
        assert result.severity == "log"
        assert result.bbox is None
        assert "pinkeye.md" in result.reasoning


    def test_rule_fallback_fires_when_raw_path_nonexistent() -> None:
        """frame_meta with a raw_path that doesn't exist on disk -> rule fallback -> bbox is None."""
        cow = _make_cow(ocular_discharge=0.85)
        result = _HEAD.classify(cow, {"raw_path": Path("/nonexistent/xxx.png")})
        assert result is not None
        assert result.severity == "escalate"
        assert result.bbox is None
    ```

    Add `from pathlib import Path` under the existing `from __future__ import annotations` line (if not already present — it probably isn't).
  </action>
  <verify>
    <automated>uv run pytest tests/vision/test_heads/test_pinkeye.py -v</automated>
  </verify>
  <done>
    - `uv run pytest tests/vision/test_heads/test_pinkeye.py -v` PASSES 11 tests (9 original + 2 new).
    - `grep -c "def test_" tests/vision/test_heads/test_pinkeye.py` returns 11.
    - No original test body was modified (verified by running each original test name individually).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/vision/test_heads/test_pinkeye_pixel.py — pixel path + determinism + latency</name>
  <files>tests/vision/test_heads/test_pinkeye_pixel.py</files>
  <behavior>
    - Test 1 `test_positive_frame_triggers_non_healthy_detection` — uses `rendered_positive_frame` fixture. `Pinkeye().classify(sick_cow, {"raw_path": png_path})` returns a non-None DetectionResult with `severity in {"watch", "log", "escalate"}` and `bbox is not None`. Bbox coords satisfy `0 <= x0 < x1 <= 640` and `0 <= y0 < y1 <= 480`. Reasoning contains `"pinkeye.md"` AND the cow tag `"SICK01"`.
    - Test 2 `test_negative_frame_returns_none` — uses `rendered_negative_frame`. For each healthy cow in world_healthy, `classify` returns None (the `should_evaluate` gate closes at `discharge <= 0.4`).
    - Test 3 `test_inference_is_deterministic` — uses `rendered_positive_frame`. Two back-to-back `classify` calls return results whose `.model_dump(exclude={'timestamp'})` dicts are equal.
    - Test 4 `test_inference_under_500ms_cpu` — uses `rendered_positive_frame`. Warm the model once, then measure wall-clock for 5 successive classify calls. Median < 500ms. Uses `time.perf_counter()`; no `pytest-benchmark` required (avoids a new dep).
    - Test 5 `test_imports_are_license_clean` — AST-grep test that reads `src/skyherd/vision/heads/pinkeye.py` source and asserts it does not import `ultralytics` or `yolov5`. Belt-and-suspenders on top of Plan 01's top-level license guard.
    - Test 6 `test_model_loads_via_lru_cache` — calls `_get_model()` twice; asserts it returns the same object identity (proves lru_cache is active).
    - No `pytest.importorskip("cv2", ...)` needed — pixel inference path does not use `supervision.BoxAnnotator`.
  </behavior>
  <action>
    Create `tests/vision/test_heads/test_pinkeye_pixel.py`. Full structure:

    ```
    """Pixel-path tests for the Pinkeye head (VIS-01, VIS-02, VIS-04)."""

    from __future__ import annotations

    import ast
    import time
    from pathlib import Path
    from statistics import median

    from skyherd.vision.heads.pinkeye import Pinkeye, _get_model
    from skyherd.world.world import World

    _HEAD = Pinkeye()


    def test_positive_frame_triggers_non_healthy_detection(
        rendered_positive_frame: tuple[Path, World],
    ) -> None:
        """Pixel path on a sick cow returns a detection with bbox set."""
        raw_path, world = rendered_positive_frame
        sick = world.herd.cows[0]
        frame_meta = {"raw_path": raw_path, "trough_id": "trough_a"}
        result = _HEAD.classify(sick, frame_meta)
        assert result is not None, "pixel path returned None for a positive frame"
        assert result.severity in {"watch", "log", "escalate"}, result.severity
        assert result.bbox is not None
        x0, y0, x1, y1 = result.bbox
        assert 0 <= x0 < x1 <= 640, f"invalid x coords: {result.bbox}"
        assert 0 <= y0 < y1 <= 480, f"invalid y coords: {result.bbox}"
        assert "pinkeye.md" in result.reasoning
        assert "SICK01" in result.reasoning


    def test_negative_frame_returns_none(
        rendered_negative_frame: tuple[Path, World],
    ) -> None:
        """Pixel path on healthy cows returns None (should_evaluate gate closes)."""
        raw_path, world = rendered_negative_frame
        frame_meta = {"raw_path": raw_path, "trough_id": "trough_a"}
        for cow in world.herd.cows:
            result = _HEAD.classify(cow, frame_meta)
            assert result is None, f"healthy cow {cow.tag} yielded detection: {result}"


    def test_inference_is_deterministic(
        rendered_positive_frame: tuple[Path, World],
    ) -> None:
        """Two back-to-back classify calls on identical input produce equal results."""
        raw_path, world = rendered_positive_frame
        sick = world.herd.cows[0]
        frame_meta = {"raw_path": raw_path, "trough_id": "trough_a"}
        r1 = _HEAD.classify(sick, frame_meta)
        r2 = _HEAD.classify(sick, frame_meta)
        assert r1 is not None and r2 is not None
        d1 = r1.model_dump(exclude={"timestamp"})
        d2 = r2.model_dump(exclude={"timestamp"})
        assert d1 == d2, f"non-deterministic inference: {d1} vs {d2}"


    def test_inference_under_500ms_cpu(
        rendered_positive_frame: tuple[Path, World],
    ) -> None:
        """Median of 5 classify calls completes under 500ms on CPU (VIS-04)."""
        raw_path, world = rendered_positive_frame
        sick = world.herd.cows[0]
        frame_meta = {"raw_path": raw_path, "trough_id": "trough_a"}
        # Warm the model + any caches
        _HEAD.classify(sick, frame_meta)
        durations_ms: list[float] = []
        for _ in range(5):
            t0 = time.perf_counter()
            _HEAD.classify(sick, frame_meta)
            durations_ms.append((time.perf_counter() - t0) * 1000.0)
        median_ms = median(durations_ms)
        assert median_ms < 500.0, (
            f"inference median {median_ms:.1f}ms exceeds 500ms budget; durations={durations_ms}"
        )


    def test_imports_are_license_clean() -> None:
        """Pinkeye source MUST NOT import ultralytics or yolov5 (VIS-02)."""
        src_path = Path("src/skyherd/vision/heads/pinkeye.py")
        tree = ast.parse(src_path.read_text())
        forbidden = {"ultralytics", "yolov5"}
        for node in ast.walk(tree):
            if isinstance(node, ast.Import):
                for alias in node.names:
                    assert alias.name.split(".")[0] not in forbidden, alias.name
            elif isinstance(node, ast.ImportFrom):
                if node.module:
                    assert node.module.split(".")[0] not in forbidden, node.module


    def test_model_loads_via_lru_cache() -> None:
        """_get_model is cached — two calls return the same object."""
        m1 = _get_model()
        m2 = _get_model()
        assert m1 is m2, "lru_cache not hit — model reloaded on second call"
        assert m1 is not None, "Plan 03 weights missing — model failed to load"
    ```

    Notes for executor:
    - The fixtures `rendered_positive_frame` and `rendered_negative_frame` live in `tests/vision/conftest.py` (Plan 02). pytest auto-discovers them.
    - The latency test warms the model BEFORE measurement — first-call load cost (150-400ms per PATTERNS.md / RESEARCH.md Pitfall 4) is excluded.
    - Do not import `ultralytics` or `yolov5` in this test file (would defeat `test_imports_are_license_clean` for itself if expanded later).
    - Do not add `pytest.importorskip("cv2", ...)` — none of these tests use cv2.
  </action>
  <verify>
    <automated>uv run pytest tests/vision/test_heads/test_pinkeye_pixel.py -v</automated>
  </verify>
  <done>
    - `tests/vision/test_heads/test_pinkeye_pixel.py` exists with 6 tests, all PASS.
    - `uv run pytest tests/vision/test_heads/ -v` PASSES all head tests (original 8 files + new pixel file).
    - `grep -c "pinkeye.md" tests/vision/test_heads/test_pinkeye_pixel.py` returns >= 1.
    - Latency test is green on the dev-box (median < 500ms).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/scenarios/test_sick_cow.py with pinkeye bbox assertion + fill 02-VALIDATION.md</name>
  <files>tests/scenarios/test_sick_cow.py, .planning/phases/02-vision-pixel-inference/02-VALIDATION.md</files>
  <behavior>
    - Existing sick_cow scenario test continues to PASS byte-for-byte (read the file first; only add new assertions or new test functions — do not modify existing ones).
    - Add a new test `test_sick_cow_scenario_produces_pinkeye_bbox_detection` that invokes the sick_cow scenario (via its existing test harness or directly via `ClassifyPipeline` on the scenario's sick world) and asserts at least ONE detection has `head_name == "pinkeye"`, `bbox is not None`, and bbox coords are within frame bounds.
    - If the sick_cow scenario's sick cow does not have `ocular_discharge > 0.4` (i.e., it's only flagged for respiratory disease, not pinkeye), the executor MUST NOT change the scenario definition. Instead, write the new test against a local `ClassifyPipeline` run on the `sick_pinkeye_world` fixture (Plan 02), NOT the scenario's default world. Name it `test_pinkeye_bbox_flows_through_classify_pipeline` and place it in `test_sick_cow.py` for scenario-level narrative grouping.
    - Fill `.planning/phases/02-vision-pixel-inference/02-VALIDATION.md` Per-Task Verification Map: one row per task across Plans 01-05 (11 rows total) with task ID, plan, wave, requirement ID, threat ref (if applicable), secure behavior, test type, automated command, file-exists flag, status. Set `nyquist_compliant: true`, `wave_0_complete: true`, and `Approval: pending` in the frontmatter.
  </behavior>
  <action>
    Read `tests/scenarios/test_sick_cow.py` in full. Identify the scenario's cow configuration (ocular_discharge value, disease flags). Two branches:

    BRANCH A — scenario's sick cow has `ocular_discharge > 0.4` OR `pinkeye` flag:
    Append:
    ```
    def test_sick_cow_scenario_produces_pinkeye_bbox_detection(...) -> None:
        """Running sick_cow scenario yields at least one pinkeye detection with a real bbox."""
        # Re-use existing scenario harness from the file above; capture detections from SSE/pipeline output.
        # Assert: any(d.head_name == "pinkeye" and d.bbox is not None for d in detections)
        # Assert bbox within frame bounds.
    ```

    BRANCH B — scenario's sick cow has discharge <= 0.4 AND no pinkeye flag (so pinkeye head never fires in that scenario):
    Append a different test that uses Plan 02's `sick_pinkeye_world` fixture:
    ```
    def test_pinkeye_bbox_flows_through_classify_pipeline(
        sick_pinkeye_world,
        tmp_path,
    ) -> None:
        """ClassifyPipeline on a pinkeye-positive world yields a pinkeye detection with real bbox."""
        from skyherd.vision.pipeline import ClassifyPipeline
        pipeline = ClassifyPipeline()
        result = pipeline.run(sick_pinkeye_world, "trough_a", out_dir=tmp_path)
        pinkeye_dets = [d for d in result.detections if d.head_name == "pinkeye"]
        assert pinkeye_dets, "ClassifyPipeline produced no pinkeye detections on a positive world"
        assert any(d.bbox is not None for d in pinkeye_dets), (
            "pinkeye detections have no bbox — pixel path did not engage"
        )
        for d in pinkeye_dets:
            if d.bbox is not None:
                x0, y0, x1, y1 = d.bbox
                assert 0 <= x0 < x1 <= 640
                assert 0 <= y0 < y1 <= 480
    ```

    Executor picks the correct branch by reading the scenario source. If `tests/scenarios/test_sick_cow.py` imports from `src/skyherd/scenarios/sick_cow.py`, read that file to determine the sick cow's `ocular_discharge` + `disease_flags`. Default to BRANCH B if uncertain (it always works, regardless of scenario internals — the fixture is self-contained). Add `from __future__ import annotations` at top if not present; add `from pathlib import Path` if using tmp_path type hint.

    Fixture signature — BRANCH B: `sick_pinkeye_world` is declared in `tests/vision/conftest.py`, not `tests/scenarios/conftest.py`. If conftest isolation prevents cross-directory fixture usage in this repo (typical pytest behavior — top-level conftests only propagate DOWN), the executor has 2 options:
    1. Duplicate the fixture in `tests/scenarios/conftest.py` (accept small code duplication for test-framework clarity), OR
    2. Move the 3 Plan 02 fixtures up to `tests/conftest.py` (global) — preferred for multi-directory reuse.

    Executor choice: option 2 (move to `tests/conftest.py`) only if `tests/conftest.py` already exists; otherwise option 1 (duplicate the minimal `sick_pinkeye_world` fixture into `tests/scenarios/conftest.py`). Document the choice in the SUMMARY.

    Fill `.planning/phases/02-vision-pixel-inference/02-VALIDATION.md`:

    - Change frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`.
    - Populate `## Per-Task Verification Map` with 11 rows (one per task: Plan 01 tasks 1-3, Plan 02 tasks 1-2, Plan 03 tasks 1-2, Plan 04 tasks 1-2, Plan 05 tasks 1-3). Each row filled with:
      - Task ID (e.g., `02-01-T1`)
      - Plan (`02-01` etc.)
      - Wave (1-4)
      - Requirement (VIS-0N)
      - Threat Ref (T-02-0N or `-`)
      - Secure Behavior (one-line summary)
      - Test Type (unit | integration | perf | license | determinism | scenario)
      - Automated Command (the exact `<automated>` tag content from that task)
      - File Exists (yes/Wave-N)
      - Status (pending until plan executes)
    - Populate `## Wave 0 Requirements` checklist: mark all 3 as complete (they are — Plans 01 + 02 delivered them).
    - Set `Approval: pending` (stays pending until the phase verifier signs off).
  </action>
  <verify>
    <automated>uv run pytest tests/scenarios/test_sick_cow.py -v &amp;&amp; uv run pytest tests/vision/ tests/scenarios/test_sick_cow.py tests/test_licenses.py -x &amp;&amp; grep -c "^\| 02-0[1-5]-T" .planning/phases/02-vision-pixel-inference/02-VALIDATION.md &amp;&amp; grep -c "nyquist_compliant: true" .planning/phases/02-vision-pixel-inference/02-VALIDATION.md &amp;&amp; make demo SEED=42 SCENARIO=sick_cow</automated>
  </verify>
  <done>
    - All existing sick_cow scenario tests PASS.
    - New test (`test_sick_cow_scenario_produces_pinkeye_bbox_detection` OR `test_pinkeye_bbox_flows_through_classify_pipeline`) PASSES.
    - `02-VALIDATION.md` Per-Task Map has 11 rows (`grep -c` >= 11 for `| 02-0N-T` pattern).
    - `nyquist_compliant: true` + `wave_0_complete: true` in frontmatter.
    - `make demo SEED=42 SCENARIO=sick_cow` PASSES (zero regression).
    - `uv run pytest tests/vision/ tests/scenarios/ tests/test_licenses.py -x` PASSES the full slice.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> pinkeye classify | Controlled pytest scope; no external input |
| SSE payload bbox -> frontend | Phase 5 dashboard will render these; Phase 2 only asserts shape, not render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | T (Tampering) | A weights swap that labels sick cows as healthy slips past CI | mitigate | `test_positive_frame_triggers_non_healthy_detection` asserts argmax != 0 on a known-positive fixture — would fail loudly |
| T-02-14 | D (DoS) | Pixel inference in tight loops exhausts CPU | mitigate | `test_inference_under_500ms_cpu` asserts median; CI will flag regression |
| T-02-15 | R (Repudiation) | Non-deterministic inference breaks determinism claims | mitigate | `test_inference_is_deterministic` asserts back-to-back equality; `make demo SEED=42` remains byte-identical per SCEN-02 |
</threat_model>

<verification>
Full phase-2 gate check (after this plan lands):
```
uv run pytest tests/ -x
uv run pytest --cov=src/skyherd/vision --cov-fail-under=85 tests/vision/
make demo SEED=42 SCENARIO=sick_cow
make demo SEED=42 SCENARIO=all
```

Determinism: run `make demo SEED=42 SCENARIO=sick_cow` three times, hash SSE-stream outputs after timestamp sanitization, confirm all three hashes match.
</verification>

<success_criteria>
- [ ] `tests/vision/test_heads/test_pinkeye.py` has 11 tests (9 original + 2 new rule-fallback explicit tests), all PASS.
- [ ] `tests/vision/test_heads/test_pinkeye_pixel.py` exists with 6 tests (positive, negative, determinism, latency, imports-clean, lru-cache), all PASS.
- [ ] `tests/scenarios/test_sick_cow.py` has a new test asserting pinkeye bbox flows through.
- [ ] `.planning/phases/02-vision-pixel-inference/02-VALIDATION.md` Per-Task Map populated (11 rows); frontmatter `nyquist_compliant: true`.
- [ ] Full test suite PASSES: `uv run pytest tests/ -x`.
- [ ] `make demo SEED=42 SCENARIO=sick_cow` PASSES.
- [ ] `make demo SEED=42 SCENARIO=all` PASSES (SCEN-02 zero regression).
- [ ] Latency test green on dev-box baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/02-vision-pixel-inference/02-05-SUMMARY.md` with:
- Measured median inference time from the latency test.
- Which scenario-test branch (A or B) was used + why.
- Which fixture-placement option (1 or 2) was used for sick_pinkeye_world cross-directory visibility.
- Confirmation that SCEN-02 zero-regression criterion still holds.
- Final Per-Task Verification Map row count.
</output>
