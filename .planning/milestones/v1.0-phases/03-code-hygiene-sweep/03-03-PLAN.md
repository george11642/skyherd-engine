---
phase: 03-code-hygiene-sweep
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/agents/test_cost.py
autonomous: true
requirements: [HYG-03, HYG-05]
tags: [cost, testing, coverage, billing, tdd]
must_haves:
  truths:
    - "`agents/cost.py` coverage rises from 78% to ≥90% (target ~98%)"
    - "MQTT publish callback path exercised — both success-capture and failure-swallow branches"
    - "Ledger callback path exercised — both success-capture and failure-swallow branches"
    - "`active_s` and `idle_s` property getters exercised"
    - "`run_tick_loop` iteration body exercised — ticks both tickers; exception-per-ticker swallowed without killing loop"
    - "Project-wide coverage holds ≥87% (zero regression)"
  artifacts:
    - path: "tests/agents/test_cost.py"
      provides: "4 new test classes: TestMqttPublishCallback, TestLedgerCallback, TestProperties, TestRunTickLoopBody"
      exports:
        - "TestMqttPublishCallback.test_publish_callback_called_with_topic_and_payload"
        - "TestMqttPublishCallback.test_publish_callback_failure_swallowed"
        - "TestLedgerCallback.test_ledger_callback_called_with_payload"
        - "TestLedgerCallback.test_ledger_callback_failure_swallowed"
        - "TestProperties.test_active_s_property"
        - "TestProperties.test_idle_s_property"
        - "TestRunTickLoopBody.test_loop_ticks_all_tickers"
        - "TestRunTickLoopBody.test_loop_swallows_ticker_exception"
      min_lines: 120
  key_links:
    - from: "tests/agents/test_cost.py::TestMqttPublishCallback"
      to: "src/skyherd/agents/cost.py:165-170"
      via: "callback-injection + assertion on captured topic/payload"
      pattern: "mqtt_publish_callback.*skyherd/ranch_a/cost/ticker"
    - from: "tests/agents/test_cost.py::TestLedgerCallback"
      to: "src/skyherd/agents/cost.py:174-177"
      via: "ledger callback injection"
      pattern: "ledger_callback"
    - from: "tests/agents/test_cost.py::TestRunTickLoopBody"
      to: "src/skyherd/agents/cost.py:205-216"
      via: "monkeypatched asyncio.sleep + stop_event + emit_tick wrapper"
      pattern: "run_tick_loop"
---

<objective>
Raise `src/skyherd/agents/cost.py` test coverage from **78% → ≥90%** (target ~98%) by adding 4 new test classes covering the 21 missing lines identified in 03-RESEARCH.md cost.py Coverage Plan. Addresses **HYG-03** and supports the **HYG-05** project-wide coverage gate.

Purpose: The Managed Agents $5k prize narrative hinges on the cost ticker demonstrably pausing when agents go idle. Today, 22% of `cost.py` is uncovered — specifically the MQTT publish callback, ledger callback, public property getters, and the 1-Hz `run_tick_loop` body. If the ticker silently regresses (e.g., a refactor breaks `emit_tick` on the MQTT path), judges see inconsistent billing behavior on camera. Covering these 21 lines ensures any regression lights up `make ci` before it lands.

Output: Single test file extension (`tests/agents/test_cost.py`) with 4 new classes totaling 8 test methods. No source-file changes — this plan is test-only. The existing `TestRunTickLoop` class structure (lines 145-156) is the exact convention to match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-code-hygiene-sweep/03-RESEARCH.md
@.planning/phases/03-code-hygiene-sweep/03-PATTERNS.md
@CLAUDE.md

@src/skyherd/agents/cost.py
@tests/agents/test_cost.py

<interfaces>
<!-- Read these from source before writing tests — verify exact signatures -->

Expected CostTicker surface (from src/skyherd/agents/cost.py):
```python
class CostTicker:
    def __init__(
        self,
        session_id: str,
        mqtt_publish_callback: Callable[[str, bytes], Awaitable[None]] | None = None,
        ledger_callback: Callable[[TickPayload], Awaitable[None]] | None = None,
    ): ...

    def set_state(self, state: Literal["active", "idle"]) -> None: ...

    async def emit_tick(self) -> TickPayload: ...

    @property
    def active_s(self) -> float: ...  # line 187

    @property
    def idle_s(self) -> float: ...  # line 191

    _last_tick_time: float  # private — tests manipulate to fast-forward

@dataclass
class TickPayload:
    session_id: str
    # ... other fields

async def run_tick_loop(
    tickers: list[CostTicker],
    stop_event: asyncio.Event,
) -> None: ...  # line 205-216
```

Expected MQTT topic published to (per 03-RESEARCH.md:521): `"skyherd/ranch_a/cost/ticker"`.

Test convention (from tests/agents/test_cost.py:145-156):
```python
class TestRunTickLoop:
    async def test_tick_loop_stops_on_event(self):
        t = CostTicker(session_id="loop-test-session")
        stop_event = asyncio.Event()
        stop_event.set()
        await run_tick_loop([t], stop_event)
```

`asyncio_mode = "auto"` already set in pyproject.toml:97 — no `@pytest.mark.asyncio` decorator needed.

Missing lines to cover (verified 2026-04-22 via `uv run pytest --cov`): 165-170, 174-177, 187, 191, 205-216.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add 4 new test classes covering 21 missing lines in cost.py (RED → GREEN in one commit — tests are additive, source does not change)</name>
  <files>tests/agents/test_cost.py</files>
  <behavior>
    **Class 1 — TestMqttPublishCallback (covers lines 165-170):**
    - `test_publish_callback_called_with_topic_and_payload`: inject async callback `mock_publish(topic, data)` that appends `(topic, data)` to a captured list. Construct CostTicker(session_id="sess-1", mqtt_publish_callback=mock_publish). Set state "active". Fast-forward `_last_tick_time -= 1.0` to force tick emission. Call `await t.emit_tick()`. Assert: captured list has 1 entry; topic == `"skyherd/ranch_a/cost/ticker"`; `json.loads(data.decode())["session_id"] == "sess-1"`.
    - `test_publish_callback_failure_swallowed`: inject raising callback `async def failing_publish(topic, data): raise RuntimeError("mqtt broker down")`. Construct ticker with this callback, set state active, fast-forward. `await t.emit_tick()` MUST NOT raise. Assert result is not None (TickPayload returned).

    **Class 2 — TestLedgerCallback (covers lines 174-177):**
    - `test_ledger_callback_called_with_payload`: analogous pattern — inject async callback capturing payload. Assert captured[0].session_id == "sess-3".
    - `test_ledger_callback_failure_swallowed`: raising ledger callback; emit_tick must not raise.

    **Class 3 — TestProperties (covers lines 187, 191):**
    - `test_active_s_property`: set state "active", `_last_tick_time -= 10.0`, emit_tick, assert `t.active_s >= 9.0` (allow ~1s wall-clock drift).
    - `test_idle_s_property`: set state "idle", `_last_tick_time -= 5.0`, emit_tick, assert `t.idle_s >= 4.0`.

    **Class 4 — TestRunTickLoopBody (covers lines 205-216):**
    - `test_loop_ticks_all_tickers`: create two tickers (active + default). Monkeypatch `skyherd.agents.cost.asyncio.sleep` to a `fast_sleep` that returns immediately. Spawn a task that calls `stop_event.set()` after one iteration. Wrap each ticker's `emit_tick` to count invocations. Call `await run_tick_loop([t_a, t_b], stop_event)`. Assert both counters >= 1.
    - `test_loop_swallows_ticker_exception`: single ticker whose `emit_tick` raises RuntimeError. fast_sleep. stop_event set early. Loop must complete without raising. This proves the loop's exception-guard swallows per-ticker failures.
  </behavior>
  <action>
    **STEP 1 (read source):** Read `src/skyherd/agents/cost.py` in full. Verify the actual signatures of:
    - `CostTicker.__init__` parameters (confirm `mqtt_publish_callback` / `ledger_callback` names)
    - `emit_tick` return type (TickPayload vs None)
    - `active_s` / `idle_s` property names
    - `run_tick_loop(tickers, stop_event)` signature
    - The exact MQTT topic string (if `"skyherd/ranch_a/cost/ticker"` differs from the live constant, use the live value)
    - The module path for `asyncio.sleep` monkeypatch (`skyherd.agents.cost.asyncio.sleep` or `asyncio.sleep`)

    If any of the above differ from the `<interfaces>` block, adjust the test bodies to match reality. The tests must match the SOURCE, not the interfaces doc.

    **STEP 2 (read existing test file):** Read `tests/agents/test_cost.py` in full to:
    - Confirm imports (`CostTicker`, `TickPayload`, `run_tick_loop`, `asyncio`, `json`). Add missing imports at top (per 03-PATTERNS.md:457-462: `json` likely needs to be added).
    - Confirm existing class style (e.g. `class TestXxx:` with `async def test_*(self)` bodies).

    **STEP 3 (write 4 new classes):** Append the 4 classes per the test bodies in 03-RESEARCH.md:509-622. Copy each test body VERBATIM from the research doc (the research author wrote them concretely for exactly this purpose) — adjust ONLY to match any signature drift discovered in STEP 1.

    Key notes:
    - No `@pytest.mark.asyncio` decorators (asyncio_mode=auto).
    - Use `t._last_tick_time -= N.0` to fast-forward time without waiting.
    - For fast_sleep: `async def fast_sleep(_): pass` assigned via `monkeypatch.setattr("skyherd.agents.cost.asyncio.sleep", fast_sleep)`.
    - For wrapped emit_tick: `t_a.emit_tick = wrapped_a  # type: ignore[method-assign]`.

    **STEP 4 (RED-first confirm then GREEN):** Run `uv run pytest tests/agents/test_cost.py -x --no-cov`. Because the tests are additive (no source change), they may all PASS immediately on first run — that's acceptable for test-coverage-uplift work. If any test FAILS unexpectedly, inspect: did the actual signature diverge from the interfaces doc? Fix the test body to match source.

    **STEP 5 (coverage verify):** Run:
    ```
    uv run pytest tests/agents/test_cost.py --cov=skyherd.agents.cost --cov-report=term-missing --cov-fail-under=90
    ```
    Expected outcome: coverage ≥90%. If still below, identify remaining missing lines from `--cov-report=term-missing` and add targeted tests. The 21-line plan should close the gap to ~98% per 03-RESEARCH.md:627.

    **STEP 6 (project-wide regression check):**
    ```
    uv run pytest --cov=src/skyherd --cov-fail-under=80
    ```
    This is the hard `fail_under=80` gate; must pass. Separately, informally verify project coverage >= 87% via the term report.

    Implements HYG-03 (cost.py ≥90%) and supports HYG-05 (zero project-wide coverage regression).
  </action>
  <verify>
    <automated>uv run pytest tests/agents/test_cost.py --cov=skyherd.agents.cost --cov-report=term-missing --cov-fail-under=90 --no-cov-on-fail</automated>
  </verify>
  <done>cost.py file coverage ≥90% (confirmed by `--cov-fail-under=90` gate); 8 new test methods all passing; project-wide coverage gate (`fail_under=80`) still passes; no source file modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → source | No source mutation; tests exercise existing code paths only. No trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering (coverage gaming) | New tests | mitigate | Each test asserts BEHAVIOR (captured topic/payload content, property values, loop-iteration counts), not just "no exception raised". Smoke-coverage gaming is explicitly avoided. |
| T-03-10 | Denial of Service (flaky tests) | `test_active_s_property` / `test_idle_s_property` with wall-clock drift | accept | Tolerance `>= 9.0` / `>= 4.0` absorbs CI-runner jitter. If jitter exceeds ~1s (extremely slow CI), this test could flake — acceptable risk vs over-mocking time. |
</threat_model>

<verification>
- `uv run pytest tests/agents/test_cost.py --cov=skyherd.agents.cost --cov-fail-under=90 --cov-report=term-missing` → exit 0.
- `uv run pytest --cov=src/skyherd --cov-fail-under=80` → exit 0 (global gate).
- No changes under `src/` — only `tests/agents/test_cost.py` modified.
- 8 new test methods all pass independently: `uv run pytest tests/agents/test_cost.py::TestMqttPublishCallback tests/agents/test_cost.py::TestLedgerCallback tests/agents/test_cost.py::TestProperties tests/agents/test_cost.py::TestRunTickLoopBody -v --no-cov`.
</verification>

<success_criteria>
- cost.py file-level coverage ≥90% (target ~98%).
- 4 new test classes / 8 new test methods committed and passing.
- Project-wide coverage holds ≥87% (informational) and ≥80% (hard gate).
- No source file modified.
- HYG-03 closed; HYG-05 gate held.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-hygiene-sweep/03-03-SUMMARY.md` including:
- Baseline coverage → final coverage deltas (per-file + project-wide)
- Which lines remain uncovered (if any) with rationale
- Zero-source-change confirmation
</output>
