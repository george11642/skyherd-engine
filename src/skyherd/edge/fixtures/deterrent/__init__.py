"""Deterrent audio fixtures for :mod:`skyherd.edge.speaker_bridge`.

The bundled ``predator_12khz.wav`` is generated deterministically by
:mod:`_generate` using stdlib only (:mod:`wave` + plain math).  Re-generating
produces byte-identical output — useful as a determinism check.
"""
