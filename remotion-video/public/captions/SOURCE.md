# captions/ — Asset Provenance

All caption files in this directory were generated or styled by the SkyHerd
video pipeline. Base captions from ElevenLabs word-level timestamps; styled
variants authored by Claude Opus 4.7 per Phase G of the demo video pipeline
(2026-04-24).

| File | Type | Model / Source | Date |
|------|------|---------------|------|
| captions-A.json | Base word-level captions | ElevenLabs timestamps, script variant A | 2026-04-22 |
| captions-B.json | Base word-level captions | ElevenLabs timestamps, script variant B | 2026-04-22 |
| captions-C.json | Base word-level captions | ElevenLabs timestamps, script variant C | 2026-04-23 |
| styled-captions-A.json | Styled captions | Claude Opus 4.7 (claude-opus-4-7), variant A — see `variant`/`model`/`fingerprint` fields in file | 2026-04-24 |
| styled-captions-B.json | Styled captions | Claude Opus 4.7 (claude-opus-4-7), variant B — see `variant`/`model`/`fingerprint` fields in file | 2026-04-24 |
| styled-captions-C.json | Styled captions | Claude Opus 4.7 (claude-opus-4-7), variant C — see `variant`/`model`/`fingerprint` fields in file | 2026-04-24 |

## Styled captions metadata

Each `styled-captions-{A,B,C}.json` embeds a JSON header block with keys:
- `variant` — A, B, or C
- `mode` — "styled"
- `model` — "claude-opus-4-7"
- `fingerprint` — SHA-256 of the generation inputs for reproducibility

These files were generated by `scripts/generate_kinetic_captions.py` with
Opus 4.7 caption styling (Phase G, video pipeline).

## License

Original work. MIT License. See root `LICENSE` file.
